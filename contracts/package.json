{
  "name": "trade-easy-contracts",
  "version": "1.0.0",
  "description": "Trade Easy — Horizen dApp contracts + private agentic trading vault",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:factory": "hardhat run scripts/deployFactory.js --network horizenTestnet",
    "deploy:vault": "hardhat run scripts/deployVault.js --network horizenTestnet",
    "deploy:staking": "hardhat run scripts/deployStaking.js --network horizenTestnet",
    "deploy:all": "npm run deploy:factory && npm run deploy:vault",
    "deploy": "hardhat run scripts/deploy.js --network horizenTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.3"
  },
  "dependencies": {
    "@hashgraph/sdk": "^2.81.0",
    "@openzeppelin/contracts": "^5.6.1"
  }
}
